const List = [89,'89',89.89,'ALX'];

List.forEach(list=> {
    if ((Number(list))) {
        console.log(`My number: ${list} `);
    }else {
        console.log(`Not a number: ${list} `);
    }

});

